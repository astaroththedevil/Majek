let numbers = [1, 2, 3, 4, 5];

console.log(numbers);

numbers.unshift(-1, 0)

console.log(numbers);

const deleted = numbers.shift();

console.log(deleted);
console.log(numbers);

const colors = ['red', 'green', 'blue'];
console.log(colors);

colors.push('yellow')
console.log(colors);

const deletedColors = colors.pop();
console.log(colors);